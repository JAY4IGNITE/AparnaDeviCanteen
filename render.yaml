services:
  # 1. Node.js Backend (Web Service)
  - type: web
    name: foodnest-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: SUPABASE_URL
        sync: false # Render will prompt you for this value during deployment
      - key: SUPABASE_SERVICE_ROLE_KEY
        sync: false # Render will prompt you for this value during deployment
      - key: JWT_SECRET
        generateValue: true # Render will automatically generate a secure secret

  # 2. React/Vite Frontend (Static Site)
  - type: web
    name: foodnest-frontend
    env: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: https://foodnest-backend.onrender.com/api
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
